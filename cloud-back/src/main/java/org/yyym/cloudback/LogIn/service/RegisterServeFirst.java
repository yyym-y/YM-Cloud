package org.yyym.cloudback.LogIn.service;

import org.yyym.cloudback.LogIn.cclass.RegisterInput;
import org.yyym.cloudback.general.Result;

public interface RegisterServeFirst {
    Result serve(RegisterInput input);
}
