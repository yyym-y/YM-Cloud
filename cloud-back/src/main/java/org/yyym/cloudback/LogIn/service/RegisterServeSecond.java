package org.yyym.cloudback.LogIn.service;

import org.yyym.cloudback.LogIn.cclass.RegisterSave;
import org.yyym.cloudback.general.Result;

public interface RegisterServeSecond {
    Result serve(RegisterSave input);
}
